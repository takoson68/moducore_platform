今天完成項目

後端：新增最小 Auth API（/api/login、/api/logout、/api/session），支援 mock token 驗證與 X-Project 檢查；補讀 Authorization header
後端入口：將 API 入口改為 api.php，並修正 .htaccess 讓 /api 正確導向
前端：project‑a 加入登入 UI（浮層+遮罩），預填測試帳密
前端：建立 src/app/api/ 組裝層，統一帶 token / X-Project；401 自動登出
前端：登入流程改用 authApi，並將 session/token 邏輯移回 api 層
尚未完成

平台能力註冊（authApi 需透過 container.register/resolve）
authStore 作為唯一登入狀態來源，Project 只能呼叫 authStore
world rebuild（登入/登出觸發的 routes/modules/stores 重建）
platformConfig 依登入狀態切換 public/auth world


---

[明日建議]
1. 依本文件把 authApi 註冊為 Platform Capability（container.register / container.resolve），移出 Project 直接 import 的使用方式。
2. 建立 authStore 作為平台唯一登入狀態來源，並改由 authStore 呼叫 authApi（Project 只呼叫 authStore.login/logout）。
3. 補上 world rebuild 流程（登入/登出觸發）：重建 modules / stores / routes，嚴禁整頁 reload。
4. 規範 Project 僅做 Login UI（移除 project-a 內的 API 調用，改成只呼叫 authStore）。
5. 釐清 platformConfig 依登入狀態切換 public/auth world 的規則與 allowList 注入點。
